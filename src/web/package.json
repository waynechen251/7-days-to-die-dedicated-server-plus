{
  "name": "7-days-to-die-dedicated-server-plus",
  "version": "1.0.5",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "pack": "pkg server.js --targets node18-win-x64 --output server.exe",
    "genMD5": "node gen-md5.js",
    "setup": "node ../inno-setup/update-version.js && cd /d ../inno-setup && iscc Setup.iss",
    "release": "npm run pack && npm run setup && npm run genMD5"
  },
  "keywords": [],
  "author": "waynechen251",
  "license": "GPL-3.0-only",
  "dependencies": {
    "archiver": "^6.0.2",
    "dayjs": "^1.11.13",
    "express": "^5.1.0",
    "extract-zip": "^2.0.1",
    "telnet-client": "^2.2.6",
    "tree-kill": "^1.2.2",
    "yauzl": "^3.2.0"
  },
  "devDependencies": {
    "pkg": "^5.8.1"
  }
}
